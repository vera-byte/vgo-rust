database:
  database: vgo_db_yaml
  host: localhost
  password: password
  port: 33061
  username: root
features:
  enable_cache: true
  enable_metrics: false
  max_connections: 100
logging:
  file: logs/app.log
  level: info
redis:
  prefix: "vgo:"
  url: redis://localhost:6379
server:
  debug: true
  host: 127.0.0.1
  port: 8080
  timeout: 30
